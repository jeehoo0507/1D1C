### 수학적 이해

---

소프트맥스 회귀(Softmax Regression)는 클래스가 3개 이상인 ==다중 클래스 분류==(Multi-class Classification)에 사용된다.

로지스틱 회귀와 완전히 별개의 모델이 아니라 ==로지스틱 회귀를 K개 클래스로 일반화한 것==이다. 그래서 다항 로지스틱 회귀(Multinomial Logistic Regression)라고도 부른다.

실제로 뒤에서 보겠지만 $K=2$를 넣으면 로지스틱 회귀 식이 그대로 튀어나온다.

### 식 유도

---

#### 풀고 싶은 문제

```latex
이미지 하나를 보고 "개인가, 고양이인가, 새인가?"를 맞히고 싶다.
```

로지스틱 회귀 때는 클래스가 2개라서 "스팸일 확률 $p$" 하나만 구하면 됐습니다. 나머지는 $1-p$로 자동으로 정해지니까요.

근데 이제 클래스가 3개입니다. 확률이 3개 필요합니다. $$p_{\text{개}}, \quad p_{\text{고양이}}, \quad p_{\text{새}}$$

그럼 선형식도 클래스마다 하나씩 만들면 되겠네요. $$z_1 = \beta_1^\top x, \quad z_2 = \beta_2^\top x, \quad z_3 = \beta_3^\top x$$

각 클래스가 ==자기만의 가중치 벡터==를 하나씩 갖는 겁니다. "개다움 점수", "고양이다움 점수", "새다움 점수"를 각각 계산하는 거라고 보면 됩니다.

#### 문제점

로지스틱 회귀 때와 똑같은 문제가 생깁니다. $z_k$는 $(-\infty, \infty)$ 인데 확률은 $(0,1)$ 이어야 하죠.

근데 이번엔 조건이 하나 더 붙습니다.

$$\text{(1)} \quad 0 < p_k < 1 \qquad \text{(2)} \quad \sum_{k=1}^{K} p_k = 1$$

==(2)가 새로 생긴 제약==입니다. "개이거나 고양이거나 새 셋 중 하나는 반드시 맞다"는 뜻이죠. 이진 분류에서는 $p$와 $1-p$를 쓰니까 합이 1인 게 공짜로 따라왔는데, 이제는 직접 챙겨야 합니다.

그럼 어떻게 하냐. 로지스틱 회귀에서 썼던 무기를 그대로 꺼내옵니다.

#### 늘리기 - 기준 클래스 대비 오즈

로지스틱 회귀에서는 이렇게 놨었습니다.

$$\log \frac{p}{1-p} = z$$

여기서 $1-p$는 사실 ==$P(y=0)$, 즉 "다른 쪽 클래스일 확률"==입니다. 그러니까 저 식은 진짜로는 이런 뜻이었던 거죠.

$$\log \frac{P(y=1)}{P(y=0)} = z$$

"클래스 1일 확률과 클래스 0일 확률의 비"입니다. 클래스 0을 ==기준(reference)==으로 삼은 거예요.

그럼 클래스가 K개일 때도 똑같이 하면 됩니다. 아무거나 하나를 기준으로 잡고 (관례상 마지막 클래스 $K$), 나머지를 기준 대비 비율로 표현합니다.

$$\log \frac{p_k}{p_K} = z_k \qquad (k = 1, 2, \dots, K)$$

이걸 $K$개 다 세워두는 겁니다. 로그 오즈비(log odds ratio)를 선형식으로 놓는다는 아이디어가 완전히 동일합니다.

#### 전개

지수를 취해서 로그를 벗깁니다.

$$\frac{p_k}{p_K} = e^{z_k}$$ $$p_k = p_K , e^{z_k}$$

아직 $p_K$를 모르죠. 근데 우리한테는 아까 그 제약 (2)가 있습니다. 양변을 $k$에 대해 다 더해봅니다.

$$\sum_{k=1}^{K} p_k = p_K \sum_{k=1}^{K} e^{z_k}$$

좌변은 제약에 의해 1입니다.

$$1 = p_K \sum_{k=1}^{K} e^{z_k}$$ $$p_K = \frac{1}{\sum_{j=1}^{K} e^{z_j}}$$

이걸 다시 $p_k = p_K e^{z_k}$ 에 대입하면

$$\boxed{p_k = \frac{e^{z_k}}{\sum_{j=1}^{K} e^{z_j}}}$$

==합이 1이라는 제약이 분모를 결정해준 것==입니다. 분모는 그냥 "전체 합"이고, 각자 자기 몫이 전체에서 차지하는 비율을 확률로 삼는 구조입니다.

### 소프트맥스 함수(Softmax function)

---

위에서 구한 식을 소프트맥스 함수라 부릅니다.

$$\text{softmax}(z)_k = \frac{e^{z_k}}{\sum_{j=1}^{K} e^{z_j}}$$

시그모이드는 스칼라 하나를 받아 스칼라 하나를 뱉지만, 소프트맥스는 ==벡터를 받아 벡터를 뱉습니다==. $K$개 값이 서로를 참조해야(분모 공유) 하니까 낱개로는 계산이 안 됩니다.

숫자로 보면 $$z = [1, 2, 3] ;\longrightarrow; p \approx [0.09,\ 0.24,\ 0.67]$$ $$z = [1, 1, 1] ;\longrightarrow; p = [0.33,\ 0.33,\ 0.33]$$

#### 왜 하필 지수함수인가

"그냥 $p_k = z_k / \sum_j z_j$ 로 나누면 안 되나?" 싶을 수 있습니다. 안 됩니다.

1. $z_k$가 음수면 확률이 음수가 됩니다
2. 분모 $\sum z_j$ 가 0이 되면 터집니다

$e^{z}$ 는 ==항상 양수==라서 이 둘을 동시에 해결합니다. 로지스틱 회귀에서 오즈가 $(0,\infty)$ 였던 것과 같은 이유죠.

덤으로 지수는 큰 값을 더 크게 벌려줍니다. 그래서 이름이 "soft"-max입니다. 진짜 max처럼 최댓값만 1로 만들고 나머지를 0으로 죽이는 게 아니라, ==최댓값 쪽으로 부드럽게 몰아주는== 함수라는 뜻입니다.

#### 성질 1 - 시프트 불변성

$z$ 전체에 아무 상수 $c$를 더해도 결과가 안 바뀝니다.

$$\frac{e^{z_k + c}}{\sum_j e^{z_j + c}} = \frac{e^c \cdot e^{z_k}}{e^c \sum_j e^{z_j}} = \frac{e^{z_k}}{\sum_j e^{z_j}} = p_k$$

==소프트맥스는 절대적인 크기가 아니라 값들 사이의 차이만 본다==는 뜻입니다. $[1,2,3]$ 이든 $[101,102,103]$ 이든 결과가 똑같습니다.

이 성질은 뒤에 나올 수치 안정성의 근거가 됩니다.

#### 성질 2 - K=2면 시그모이드

클래스가 2개일 때 소프트맥스를 풀어보면

$$p_1 = \frac{e^{z_1}}{e^{z_1} + e^{z_2}}$$

분자·분모를 $e^{z_1}$ 로 나누면

$$= \frac{1}{1 + e^{z_2 - z_1}} = \frac{1}{1 + e^{-(z_1 - z_2)}} = \sigma(z_1 - z_2)$$

==정확히 시그모이드==입니다. 유도 과정에서 기준 클래스를 잡았던 걸 떠올리면, $z_2 = 0$ 으로 고정한 게 로지스틱 회귀였던 셈입니다.

#### 성질 3 - 온도(temperature)

지수 안을 $T$로 나누면

$$p_k = \frac{e^{z_k / T}}{\sum_j e^{z_j / T}}$$

- $T \to 0$ : 가장 큰 것만 1, 나머지 0 (hard max = argmax)
- $T = 1$ : 원래 소프트맥스
- $T \to \infty$ : 전부 균등하게 $1/K$

LLM에서 말하는 temperature 파라미터가 정확히 이겁니다. 높이면 다양한 답, 낮추면 뻔한 답이 나오는 이유죠.

### 분류

---

로지스틱 회귀에서는 0.5를 기준으로 잘랐지만, 소프트맥스에서는 그냥 ==가장 확률이 큰 클래스==를 고릅니다.

$$\hat{y} = \arg\max_k p_k$$

그런데 $e^x$ 는 단조 증가 함수이고 분모는 모두 공통이므로

$$\arg\max_k p_k = \arg\max_k z_k$$

==추론할 때는 소프트맥스를 통과시킬 필요조차 없습니다.== 로짓 중에서 제일 큰 걸 고르면 끝입니다. 확률값 자체가 궁금할 때만(신뢰도 표시, 임계값 설정 등) 소프트맥스를 씁니다.

물론 로지스틱 회귀 때처럼 임계값을 따로 두는 경우도 있습니다. 예를 들어 "최대 확률이 0.6 미만이면 판단 보류" 같은 식으로요.

### 손실

---

#### 쉬움

---

로지스틱 회귀의 BCE를 그대로 확장하면 됩니다.

BCE는 이랬습니다. $$\text{cost} = -\left[y \log p + (1-y)\log(1-p)\right]$$

여기서 $y$와 $1-y$는 사실 =="정답 클래스에만 1을 켜주는 스위치"== 역할이었습니다. 정답이 1이면 앞항만 살고, 정답이 0이면 뒷항만 살죠.

클래스가 K개면 스위치도 K개 필요합니다. 그래서 정답을 ==원-핫 벡터==로 표현합니다.

$$y = [0, 0, 1, 0] \quad (\text{정답이 3번 클래스})$$

그럼 손실은 $$\text{cost} = -\sum_{k=1}^{K} y_k \log p_k$$

원-핫이라 항이 딱 하나만 살아남습니다. 정답 인덱스를 $t$라 하면

$$\boxed{\text{cost} = -\log p_t}$$

==정답 클래스에 부여한 확률에만 로그를 씌운 것==입니다. 정답에 0.99를 줬으면 손실이 거의 0, 0.01을 줬으면 손실이 매우 큽니다. 이걸 크로스 엔트로피(Cross Entropy)라고 부릅니다.

#### 심화 - MLE로 유도하기

---

로지스틱 회귀에서 했던 것과 똑같은 흐름입니다.

이진일 때는 $P(y \mid x)$ 를 이렇게 썼었죠.

$$P(y \mid x) = p^{y}(1-p)^{1-y}$$

지수를 스위치로 쓰는 트릭이었습니다. 클래스가 K개면 자연스럽게

$$P(y \mid x) = \prod_{k=1}^{K} p_k^{,y_k}$$

정답이 3번이면 $y = [0,0,1,0]$ 이니까

$$p_1^0 \cdot p_2^0 \cdot p_3^1 \cdot p_4^0 = p_3$$

의도한 대로 ==정답 클래스의 확률만 뽑혀 나옵니다.==

$m$개 샘플 전체의 우도는 곱으로

$$L = \prod_{i=1}^{m} \prod_{k=1}^{K} p_{ik}^{,y_{ik}}$$

로그를 취하면 (이유는 로지스틱 때와 동일: 값이 너무 작아짐 / 곱의 미분이 지옥 / 지수 형태 미분 어려움)

$$\ell = \log L = \sum_{i=1}^{m} \sum_{k=1}^{K} y_{ik} \log p_{ik}$$

부호를 뒤집고 $m$으로 나누면 최종 비용함수가 됩니다.

$$\boxed{J = -\frac{1}{m}\sum_{i=1}^{m} \sum_{k=1}^{K} y_{ik} \log p_{ik}}$$

### 경사하강법

---

$\beta$ 가 $J$ 까지 가는 경로는 이렇습니다.

$$\beta_{kj} ;\to; z_k ;\to; p ;\to; J$$

로지스틱 때와 딱 하나 다른 게 있습니다. ==$z_j$ 하나를 건드리면 모든 $p_k$ 가 다 흔들린다==는 점입니다. 분모 $\sum_j e^{z_j}$ 를 전부 공유하고 있으니까요.

그래서 체인룰에 시그마가 하나 더 붙습니다.

$$\frac{\partial \ell}{\partial z_j} = \sum_{k=1}^{K} \frac{\partial \ell}{\partial p_k} \cdot \frac{\partial p_k}{\partial z_j}$$

#### 1. $\partial \ell / \partial p_k$

$\ell = \sum_k y_k \log p_k$ 이므로 그냥

$$\boxed{\frac{\partial \ell}{\partial p_k} = \frac{y_k}{p_k}}$$

#### 2. $\partial p_k / \partial z_j$ — 야코비안

표기를 줄이려고 분모를 $S$로 둡니다.

$$S = \sum_{j=1}^{K} e^{z_j}, \qquad p_k = \frac{e^{z_k}}{S}, \qquad \frac{\partial S}{\partial z_j} = e^{z_j}$$

$S$가 $z_j$를 포함하고 있다는 게 핵심입니다. 여기서 ==$k$와 $j$가 같은지 다른지에 따라 결과가 갈립니다.==

**(a) $k = j$ 인 경우** (분자에도 $z_j$가 있음)

몫의 미분 $\left(\frac{f}{g}\right)' = \frac{f'g - fg'}{g^2}$ 을 쓰면

$$\frac{\partial p_k}{\partial z_k} = \frac{e^{z_k} \cdot S - e^{z_k} \cdot e^{z_k}}{S^2}$$

$$= \frac{e^{z_k}}{S} \cdot \frac{S - e^{z_k}}{S} = \frac{e^{z_k}}{S}\left(1 - \frac{e^{z_k}}{S}\right)$$

$$\boxed{\frac{\partial p_k}{\partial z_k} = p_k(1-p_k)}$$

시그모이드 미분 $\sigma'= p(1-p)$ 와 ==똑같은 꼴==이 나옵니다.

**(b) $k \neq j$ 인 경우** (분자에는 $z_j$가 없음, 분모에만 있음)

분자 $e^{z_k}$ 는 $z_j$ 에 대해 상수이므로 $f' = 0$

$$\frac{\partial p_k}{\partial z_j} = \frac{0 \cdot S - e^{z_k}\cdot e^{z_j}}{S^2} = -\frac{e^{z_k}}{S}\cdot\frac{e^{z_j}}{S}$$

$$\boxed{\frac{\partial p_k}{\partial z_j} = -p_k p_j}$$

==음수라는 게 의미심장합니다.== 한 클래스의 점수를 올리면 다른 클래스의 확률은 반드시 내려갑니다. 분모를 공유하는 구조라 서로 경쟁하는 거죠. 확률 총합이 1로 묶여 있으니 당연한 결과이기도 합니다.

두 경우를 크로네커 델타로 한 줄에 쓰면

$$\frac{\partial p_k}{\partial z_j} = p_k(\delta_{kj} - p_j), \qquad \delta_{kj} = \begin{cases}1 & k=j \ 0 & k \neq j\end{cases}$$

#### 3. 합치기

$$\frac{\partial \ell}{\partial z_j} = \sum_{k} \frac{y_k}{p_k}\cdot p_k(\delta_{kj} - p_j)$$

$p_k$ 가 약분됩니다. (로지스틱 회귀에서 $p(1-p)$ 가 통째로 약분됐던 것과 같은 현상)

$$= \sum_k y_k(\delta_{kj} - p_j)$$

시그마를 $k=j$ 항과 나머지로 쪼개면

$$= y_j(1 - p_j) + \sum_{k \neq j} y_k(0 - p_j)$$ $$= y_j - y_j p_j - p_j\sum_{k \neq j} y_k$$

여기서 원-핫이므로 $\sum_k y_k = 1$, 따라서 $\sum_{k \neq j} y_k = 1 - y_j$

$$= y_j - y_j p_j - p_j(1 - y_j)$$ $$= y_j - y_j p_j - p_j + p_j y_j$$

$$\boxed{\frac{\partial \ell}{\partial z_j} = y_j - p_j}$$

==로지스틱 회귀와 완전히 동일한 형태==입니다. 야코비안이 그렇게 복잡했는데 결국 오차 하나로 정리됩니다.

이게 우연이 아닌 이유는, 시그모이드+BCE도 소프트맥스+CE도 ==(지수족 분포) + (그에 맞는 로그 손실)== 조합이라 미분의 복잡한 항이 항상 약분되도록 설계되어 있기 때문입니다.

#### 4. 파라미터까지

$z_j = \beta_{j0} + \beta_{j1}x_1 + \cdots$ 이므로

$$\frac{\partial z_j}{\partial \beta_{jl}} = x_l$$

전부 합치고 샘플에 대해 더하면

$$\frac{\partial \ell}{\partial \beta_{jl}} = \sum_{i=1}^{m}(y_{ij} - p_{ij}),x_{il}$$

로지스틱 때처럼 데이터 개수 의존을 없애려고 $m$으로 나누고, 부호를 뒤집어 비용함수 기준으로 쓰면

$$\frac{\partial J}{\partial \beta_{jl}} = -\frac{1}{m}\sum_{i=1}^{m}(y_{ij} - p_{ij}),x_{il} = \frac{1}{m}\sum_{i=1}^{m}(p_{ij} - y_{ij}),x_{il}$$

$$\beta ;\leftarrow; \beta - \lambda \frac{\partial J}{\partial \beta}$$

### 행렬로 정리

---

|기호|shape|행 / 열 의미|
|---|---|---|
|$X$|$m \times n$|샘플 / 피처|
|$W$|$K \times n$|출력 클래스 / 입력 피처|
|$Z = XW^\top + b$|$m \times K$|샘플 / 클래스|
|$P = \text{softmax}(Z)$|$m \times K$|행마다 합이 1|
|$Y$ (원-핫)|$m \times K$|샘플 / 클래스|

$$\frac{\partial J}{\partial W} = \frac{1}{m}(P - Y)^\top X \qquad \underbrace{(K \times m)}_{}\underbrace{(m \times n)}_{} = K \times n$$

행렬곱에서 ==샘플 축 $m$이 사라지는 게 곧 "샘플에 대해 합산했다"==는 뜻입니다.

주의할 점 하나. 소프트맥스는 ==행 방향(클래스 축)으로만== 적용됩니다. 각 샘플이 독립적으로 자기 K개 클래스에 대해 분포를 만드는 거니까요.

```python
F.softmax(z, dim=1)   # 맞음: 클래스 축으로 정규화
F.softmax(z, dim=0)   # 틀림: 샘플 축으로 정규화됨 (조용히 망가짐)
```

에러가 안 나고 그냥 학습이 안 되는 유형이라 찾기 어렵습니다.

### 수치 안정성

---

$z_k$ 가 크면 $e^{z_k}$ 가 오버플로 납니다. float32에서 $e^{89}$ 쯤이면 이미 `inf` 입니다.

여기서 아까 그 ==시프트 불변성==을 씁니다. 최댓값을 빼줘도 결과가 안 변하니까

$$p_k = \frac{e^{z_k - \max_j z_j}}{\sum_j e^{z_j - \max_j z_j}}$$

이러면 지수의 인자가 전부 $\leq 0$ 이 되어 $e^{(\cdot)} \leq 1$ 로 안전합니다.

로그까지 씌울 거면 아예 합쳐서 계산합니다.

$$\log p_k = z_k - \max_j z_j - \log\sum_j e^{z_j - \max_j z_j}$$

이게 ==LogSumExp 트릭==이고, `F.log_softmax` 가 내부에서 하는 일입니다. 소프트맥스 결과가 0으로 언더플로된 다음 로그를 씌워서 $-\infty$ 가 되는 사고도 같이 막아줍니다.

### PyTorch 실전

---

가장 많이 하는 실수 구간입니다.

```python
criterion = nn.CrossEntropyLoss()
loss = criterion(z, target)   # z는 로짓! softmax 통과 전!
```

`nn.CrossEntropyLoss` = `log_softmax` + `NLLLoss` 가 ==이미 합쳐진 것==입니다. 위의 LogSumExp 트릭까지 내장되어 있고요.

|쓰는 손실 함수|모델 마지막에 softmax|비고|
|---|---|---|
|`nn.CrossEntropyLoss`|==넣지 말 것==|로짓 그대로 넘김|
|`nn.NLLLoss`|`log_softmax` 필요||
|`F.softmax` 후 직접 로그|비권장|수치 불안정|

softmax를 두 번 먹이면 분포가 뭉개져서(이미 0~1인 값에 다시 지수를 씌우니 차이가 줄어듦) 학습이 느려지거나 아예 안 됩니다. 이것도 에러 없이 성능만 나빠지는 유형입니다.

그리고 `target` 은 원-핫이 아니라 ==정수 인덱스 `(m,)`== 를 받습니다. 어차피 정답 항 하나만 쓰니까 원-핫을 만들 이유가 없어서요.

```python
target = torch.LongTensor([2, 0, 1])   # 원-핫 아님
```

### 소프트맥스 vs 시그모이드

---

||소프트맥스|시그모이드 K개|
|---|---|---|
|제약|$\sum_k p_k = 1$|각각 독립, 합 제약 없음|
|문제 유형|multi-class (택 1)|multi-label (복수 정답 가능)|
|예시|개 / 고양이 / 새 중 하나|사진 태그: 실내 + 사람 + 야간|
|손실|`CrossEntropyLoss`|`BCEWithLogitsLoss`|

판별법은 간단합니다.

```latex
"한 샘플이 두 클래스에 동시에 속할 수 있나?"
→ 있다: 시그모이드
→ 없다: 소프트맥스
```

### 번외

---

#### 파라미터가 하나 남는다

유도 초반에 $\log(p_k / p_K) = z_k$ 로 놓고 시작했습니다. 기준 클래스 $K$를 잡았으니 사실 ==$K-1$개의 $\beta$ 벡터만 있으면 충분==합니다. $z_K = 0$ 으로 고정해도 되는 거죠. 로지스틱 회귀가 정확히 이 방식입니다.

그런데 실제 구현에서는 그냥 $K$개를 다 둡니다. 시프트 불변성 때문에 $\beta$ 전체에 아무 벡터나 더해도 확률이 안 변하니, 해가 유일하지 않은 ==과대매개변수화(over-parameterization)== 상태가 됩니다.

그래도 문제가 안 되는 이유는:

- 어차피 예측 확률은 유일하게 정해짐
- 코드가 훨씬 대칭적이고 깔끔함
- 정규화(weight decay)를 걸면 해가 다시 유일해짐

#### 왜 크로스 엔트로피라 부르나

정보이론에서 두 분포 $y$, $p$ 사이의 크로스 엔트로피는

$$H(y, p) = -\sum_k y_k \log p_k$$

우리가 유도한 손실과 정확히 같습니다. 정답 분포 $y$(원-핫)와 예측 분포 $p$가 ==얼마나 다른지를 재는 척도==인 셈이고, MLE로 유도해도 정보이론으로 유도해도 같은 식이 나옵니다.

- 소프트맥스도 로컬 미니멈 없이 글로벌 미니멈만 가지는가? [[로컬 미니멈의 존재 여부]]