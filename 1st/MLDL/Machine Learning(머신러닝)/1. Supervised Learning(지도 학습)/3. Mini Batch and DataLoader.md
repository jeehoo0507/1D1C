미니 배치와 데이터 로더


우리가 실제 다루는 데이터들은 한번에 처리하기에 너무 많은 양을 가지고 있다.
그래서 데이터를 ==더 작은 단위==로 쪼개어서 학습을 진행한다.

### Chapter
- 미니 배치
- 이터레이션
- 데이터 로드
- 커스텀 데이터셋


 1. 미니 배치

![[image-13.png]]


```
미니 배치만큼만 가져가서 비용(cost)를 계산

경사 하강법을 수행

다음 미니 배치를 가져가서 경사 하강법을 수행

...

반복
```

 전체 데이터에 대한 학습이 1회를 1 에포크(Epoch)라고 하며 이는 하이퍼파라미터이다.


미니 배치의 크기 
> 배치 크기(batch size)

- 배치 크기는 cpu,gpu 메모리가 2의 배수인것을 고려하여 2의 제곱수(4,16,64...)로 잡는다.


| 전체 데이터 | 배치(한 번에)     | 미니 배치                |
| ------ | ------------ | -------------------- |
| 이름     | 배치 경사 하강법    | 미니 배치 경사 하강법         |
| 장점     | 가중치 업데이트 안정적 | 계산량, 훈련 속도           |
| 단점     | 계산량          | 가중치 업데이트에서 값을 헤매기도 함 |



2. 이터레이션(Iteration)
![[image-14.png|307x296]]

==이터레이션==
한 번의 에포크 내에서 이루어지는 매개변수인 가중치 w와 b의 업데이트 횟수




 3.  데이터 로드

파이토치에서는 데이터 로더를 통해 데이터 셔플(shuffle), 드롭아웃(drop out) 등의 기능을 지원한다.

```
dataloader = DataLoader(dataset, batch_size=2, shuffle=True)
```



4. 커스텀 데이터셋(Custom Dataset)

파이토치에서 많이 나오는 구조이기에 기억해 두어야 한다.

```
class CustomDataset(torch.utils.data.Dataset): 
  def __init__(self):
  데이터셋의 전처리를 해주는 부분

  def __len__(self):
  데이터셋의 길이. 즉, 총 샘플의 수를 적어주는 부분

  def __getitem__(self, idx): 
  데이터셋에서 특정 1개의 샘플을 가져오는 함수

```

- len(dataset)을 했을 때 데이터셋의 크기를 리턴할 **len**
- dataset[i]을 했을 때 i번째 샘플을 가져오도록 하는 인덱싱을 위한 **get_item**



