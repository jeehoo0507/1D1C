뻔하지 않은 사이드 프로젝트 주제 추천

29초 동안 생각함

29초 동안 생각함

뻔한 걸 피하는 요령은 "남의 데이터"가 아니라 "내 생활에서만 나오는 데이터"를 다루거나, 기존 도구를 이상한 축으로 재정렬하는 겁니다. 그 기준으로 몇 개 골라봤어요.

**내 데이터로 만드는 것**

- **개인 물가지수** — 영수증 OCR로 내가 실제 사는 품목만 추적해서 나만의 CPI를 만들기. 공식 물가와 체감 물가의 차이를 숫자로 보게 됨.
- **결정 로그 검색기** — 회의록/채팅에서 "~하기로 함" 같은 결정 문장만 뽑아 저장. 몇 달 뒤 "우리 왜 이렇게 했지?"를 검색으로 답하는 도구.
- **우리 집 사전** — 가족·친구 채팅 백업에서 우리끼리만 쓰는 표현을 빈도 기반으로 추출. NLP 연습 겸 꽤 웃긴 결과가 나옴.

**기존 도구를 다른 축으로 뒤집기**

- **병렬 조리 스케줄러** — 레시피를 재료가 아니라 "불 자리·손이 비는 시간" 기준으로 재정렬. 요리 3개를 동시에 할 때 크리티컬 패스를 계산해줌. 스케줄링 알고리즘 써볼 좋은 핑계.
- **약관 diff 알림** — 자주 쓰는 서비스의 이용약관을 주기적으로 긁어 git처럼 변경점만 보여주기. "약관이 변경되었습니다" 메일이 실제로 뭘 바꿨는지 알려주는 유일한 도구가 됨.
- **인용문 출처 검증기** — SNS에서 도는 명언이 정말 그 사람 말인지 원출처를 찾아주는 브라우저 확장.

**공간·물리 데이터**

- **그늘 경로 찾기** — 건물 높이 데이터와 태양 위치로 특정 시각에 그늘이 많은 도보 경로 계산. 여름에 진짜 쓰게 되는 지도 프로젝트.
- **환기 타이밍 알리미** — CO2 센서 + 창문 자석 센서만으로 "지금 열어라/닫아라"를 판단. 식물 물주기 다음 단계의 IoT.

**개발자 자신을 향한 것**

- **커밋 메시지 번아웃 지표** — 내 커밋 메시지의 어조·시간대·길이 변화를 추적해서 컨디션 저하를 조기에 잡아내기.

어떤 스택이나 관심 분야(웹, 데이터, 하드웨어 등)가 있으면 그 방향으로 더 좁혀드릴 수 있어요.

다른거 없음?

더 뽑아봤어요. 이번엔 결이 다른 것들로.

**소리·시간**

- **동네 소음 히트맵** — 폰 마이크로 데시벨을 시간대별로 기록해 지도에 겹치기. 이사 갈 때 "밤 11시 이 골목 시끄러운가"를 데이터로 답함.
- **"오늘 뭐 했지" 재구성기** — 스크린샷, 사진 메타데이터, 위치 기록, 캘린더를 합쳐 하루를 타임라인으로 자동 복원. 일기를 쓰는 게 아니라 이미 남은 흔적을 조립하는 방식.

**언어·텍스트**

- **익명 출처 비율 표시기** — 뉴스 기사에서 "관계자에 따르면", "알려졌다" 같은 표현 비율을 계산해 기사 상단에 뱃지로 표시하는 확장.
- **번역 왕복 테스트** — 내 글을 여러 언어로 왕복 번역시켜 원문이 얼마나 살아남는지 측정. 문장이 모호한 곳을 찾는 퇴고 도구로 쓸 수 있음.
- **내 어휘 성장 그래프** — 몇 년치 내 글에서 처음 등장한 단어를 시점별로 뽑아 "언제 이 단어를 배웠나"를 시각화.

**오프라인·물리**

- **한 장짜리 여행 지도 생성기** — OSM 데이터를 A4 한 장에 인쇄 최적화. 폰 배터리 없는 상황을 전제로 한 역설적 프로젝트.
- **책장 인덱서** — 책장 사진 한 장에서 책등 텍스트를 읽어 소장 목록화. 중고서점에서 "이거 집에 있던가?"를 해결.
- **냉장고 유통기한 보드** — 영수증 날짜 + 품목별 평균 보존기간으로 "이번 주에 먹어야 할 것" 자동 생성. 바코드 스캔 없이 되는 게 포인트.

**게임·놀이**

- **실제 노선도 보드게임** — 지하철 노선도를 그대로 보드로 쓰는 전략 게임. 환승역이 요충지가 되는 구조가 자연스럽게 나옴.
- **오늘의 위키 랠리** — 위키백과 무작위 두 문서를 링크만 타고 최단 경로로 잇기. 매일 문제가 바뀌는 데일리 퍼즐 형식.

**돈**

- **구독 실사용률 계산기** — 카드 내역과 앱 사용 시간을 대조해 "월 1.5만 원 내고 3번 열었음" 같은 경고를 띄움.
- **환율 적기 알림의 반대** — 언제 사야 했는지 사후 분석해서 내 환전 타이밍이 평균보다 얼마나 나빴는지 보여주는 자기반성 도구.

이 중 마음에 걸리는 게 하나라도 있으면 구현 난이도나 첫 주에 만들 MVP 범위를 같이 잡아볼 수 있어요.

네, 그게 이 툴의 핵심 설계 결정이에요. 자유도를 줄이는 게 품질을 올리는 방향입니다.

**왜 효과가 있나**

모델이 실패하는 건 "이 텍스트를 어디에 얼마나 크게 놓을까"를 매번 새로 정하기 때문이에요. 레이아웃을 고정하면 그 판단이 사라지고, 모델은 "이 내용은 2단 비교니까 `two-col`" 같은 분류만 하면 됩니다. 분류는 모델이 아주 잘하는 일이에요.

**레이아웃 정의에 들어갈 것**

이름만 정하는 게 아니라 슬롯과 한계를 같이 박아두세요.

```
two-col
  슬롯: title(1), left_heading(1), left_items(2~5), right_heading(1), right_items(2~5)
  한계: 항목당 60자, 제목 40자
  용도: 비교, 전후, 장단점
```

이 "한계"가 중요해요. 모델에게 미리 알려주면 넘침이 애초에 덜 생기고, 검증자는 렌더링 전에 글자 수만으로 1차 걸러낼 수 있어요. 렌더링은 그 다음 방어선.

**세트 구성 제안**

과제 발표라면 이 정도로 대부분 커버돼요.

- `title` — 표지
- `section` — 챕터 구분
- `bullets` — 제목 + 항목
- `two-col` — 비교/대조
- `image-caption` — 도표 + 설명
- `table` — 표
- `quote-stat` — 큰 숫자나 인용 하나 강조
- `closing` — 마무리, 참고문헌

8개면 충분하고, 처음엔 `title`, `bullets`, `two-col` 셋만으로 시작해도 돌아갑니다.

**모델에게 주는 형식**

HTML을 직접 쓰게 하지 말고 레이아웃 이름 + 슬롯 값만 채우게 하세요.

json

```json
{ "layout": "two-col",
  "title": "지도학습 vs 비지도학습",
  "left": { "heading": "지도학습", "items": ["레이블 필요", "분류·회귀"] },
  "right": { "heading": "비지도학습", "items": ["레이블 불필요", "군집화"] } }
```

이 JSON을 HTML로 바꾸는 건 툴이 결정적으로 처리해요. 모델 출력이 스키마로 검증 가능해지고, 실패해도 어디가 틀렸는지 명확합니다. 나중에 pptx 도형 매핑으로 갈 때도 레이아웃별 매핑 함수만 쓰면 되니 훨씬 쉬워져요.

**한 가지 안전장치**

`custom` 레이아웃을 하나 열어두되 사용하면 경고를 남기게 하세요. 정해둔 8종으로 안 되는 케이스가 실제로 얼마나 나오는지 로그가 쌓이고, 그게 다음 레이아웃을 뭘 추가할지 알려주는 데이터가 됩니다. 발표 자료에 "실사용 40장 중 custom 2회, 이후 `timeline` 추가" 같은 근거로 쓸 수 있어요.

mcp와 스킬즈 차이 그리고 하네스 간단 설명하기 위한

발표에서 세 개 관계를 한 문장으로 잡으면 이렇습니다.

> **하네스**가 무대, **MCP**가 무대 위의 도구, **Skill**이 대본.

각각 조금 더 풀면:

**MCP — 모델이 _할 수 있는 일_을 늘림**  
외부 시스템에 연결해 실제로 뭔가 실행하는 통로. 브라우저 렌더링, 파일 쓰기, DB 조회처럼 코드가 돌아야 하는 것. 프로토콜이라 서버 하나 만들어두면 여러 클라이언트가 꽂아 씁니다. 없으면 모델은 말만 할 수 있고 아무것도 못 해요.

**Skill — 모델이 _아는 것_을 늘림**  
실행이 아니라 지식과 절차. 슬라이드 DSL 문법, 레이아웃 카탈로그, 과목별 제출 규칙, "불릿 5개 이하" 같은 규칙. 파일로 주면 끝이고 서버가 필요 없어요. 필요할 때만 읽히니 컨텍스트도 아낍니다.

**구분 기준 한 줄**: 프로세스를 띄워야 하면 MCP, 읽으면 되면 Skill.

**하네스 — 둘을 실제로 돌아가게 하는 층**  
모델을 호출하고, 툴 호출을 받아 MCP로 넘기고, 결과를 다시 컨텍스트에 넣고, 언제 멈출지 정하고, 상태를 저장하는 루프. 모델 자체는 텍스트를 텍스트로 바꾸는 함수일 뿐이고, 하네스가 붙어야 에이전트가 됩니다. DeepSeek 표현으로는 **Model + Harness = Agent**.

**발표용 예시 하나로 묶기**

PPT 툴로 설명하면 세 개가 자연스럽게 나옵니다.

- Skill: 레이아웃 8종과 각 슬롯의 글자 수 제한 — 모델이 읽고 JSON을 채움
- MCP: `render_slides`, `check_overflow`, `export_pptx` — Playwright를 띄워야 하므로 서버
- 하네스: JSON 생성 → 렌더 → 넘침 발견 → 항목 줄여 다시 생성, 을 통과할 때까지 반복하고 3회 실패하면 사람에게 넘기는 그 루프

**한 장 슬라이드로 만들면**

||형태|늘려주는 것|예시|
|---|---|---|---|
|MCP|서버(프로토콜)|실행 능력|렌더링, 파일 저장|
|Skill|문서/파일|지식·절차|DSL 문법, 과목 규칙|
|하네스|런타임|지속·판단|루프, 컨텍스트, 권한|

자주 나오는 질문 대비로 하나 덧붙이면 — "MCP로도 지침을 줄 수 있지 않나?" 가능하지만 서버를 띄우고 유지해야 하고 버전 관리가 어려워요. 반대로 Skill에 코드를 넣어봐야 실행 주체가 없습니다. 그래서 실행/지식 축으로 나누는 게 관리 비용 면에서 이득이라고 답하면 됩니다.