### 1. 미분이 무엇이었나?
---
$f(x)=x^2$, $x=3$ 일 때 $f = 9$
$x$를 아주 미소량 0.01 정도 가정해서 밀면  $f$는 얼마나 움직일까?

$f(3.01) = 9.0601$ , 즉 0.00601 대략 0.01의 6배
이 6은 미분값이다. 의미는
>입력 밀면 출력이 몇 배로 반응하는가?

결국 미분 = 반응 비율

### 2. 입력이 두 개가 된다면?
---
함수가 아래와 같이 생겼다 하자
$$u = 2x+5y$$
$x$ 랑 $y$ 둘 다 밀 수 있음. 즉, 반응 비율이 두개 필요함

- $x$ 만 밀면? $y$는 가만히 두고 배율 2
- $y$만 밀면? $x$는 가만히 두고 배율 5

$$\frac{\partial u}{\partial x} = 2 \ ,\quad \frac{\partial u}{\partial y} = 5$$
$x$를 0.1, $y$를 0.2 만큼 움직이면?
$$\Delta u = 2(0.1) + 5(0.2) = 1.2$$

일반화 시키면
$$\Delta u \approx \frac{\partial u}{\partial x}\Delta x + \frac{\partial u}{\partial y}\Delta y$$
여기서 0.01 처럼 유한한 값으로 가정한 변화량 $\Delta$를 무한소로 두면

$$du = \frac{\partial u}{\partial x}\,dx + \frac{\partial u}{\partial y}\,dy$$
==전미분== 식이 된다.

### 3. 출력도 두 개가 된다면?
---
$$u = 2x + 5y$$
$$v = 7x + 3y$$
이제 배율이 4 개가 필요함

|       | $x$밀면 | $y$밀면 |
| ----- | ----- | ----- |
| $u$반응 | 2     | 5     |
| $v$반응 | 7     | 3     |

이 표가 ==야코비안==이다.

$$J = \begin{bmatrix} 2 & 5 \\ 7 & 3 \end{bmatrix}$$

### 4. 표 읽기
$x$를 0.1 , $y$를 0.2 밀었을 때
###### $u$ 는?
$$2(0.1) + 5(0.2) = 1.2$$
###### $v$는?
$$7(0.1)+3(0.2)$$
$$J = \begin{bmatrix}
\dfrac{\partial u}{\partial x} & \dfrac{\partial u}{\partial y} \\[6pt]
\dfrac{\partial v}{\partial x} & \dfrac{\partial v}{\partial y}
\end{bmatrix}$$
일반화하면
$$\Delta \mathbf{u} \approx J\,\Delta \mathbf{x}$$
$$\begin{bmatrix} \Delta u \\ \Delta v \end{bmatrix}
\approx
\begin{bmatrix}
\dfrac{\partial u}{\partial x} & \dfrac{\partial u}{\partial y} \\[6pt]
\dfrac{\partial v}{\partial x} & \dfrac{\partial v}{\partial y}
\end{bmatrix}
\begin{bmatrix} \Delta x \\ \Delta y \end{bmatrix}$$
### 5. 무엇을 의미하는가?
$$x \xrightarrow{} y \xrightarrow{}  z$$
$x$ 변화에 따른 $z$값 변화량 
$$\frac{\partial z}{\partial x} = \frac{\partial z}{\partial y} \cdot \frac{\partial y}{\partial x}$$
다른 관점에서 보면 $x$를 0.1 밀면 $y$가 0.2 움직이고 $z$가 0.6 움직인다.
즉, 전체 배율이 2 x 3 =  6 이다.


벡터로 확정해서 입력과 출력 개수를 늘려보면
$$x(2 \text개) \xrightarrow{} h(2 \text개) \xrightarrow{}  z(2 \text개)$$
$$\Delta\mathbf{h} \approx J_{h/x}\,\Delta\mathbf{x},
\qquad
\Delta\mathbf{z} \approx J_{z/h}\,\Delta\mathbf{h}$$
 $$\Delta\mathbf{z}
\approx J_{z/h}\,\underbrace{\left(J_{h/x}\,\Delta\mathbf{x}\right)}_{\Delta\mathbf{h}}
= \underbrace{\left(J_{z/h}\,J_{h/x}\right)}_{J_{z/x}}\Delta\mathbf{x}$$
$$\Delta\mathbf{z} \approx  J_{z/x} \Delta x$$
이 식으로 인해서
$$J_{z/x} = J_{z/h}J_{h/x}$$
체인 룰이 가능하다!!!

예를 들어 손실 함수 $L$에 대해서 가중치 $w_1$의 업데이트를 하고 싶다고 하면 기존에는 기울기를 구하면 됨 근데 이제 기울기 = 배율이고 배율 = 야코비안이 되었으니까 체인룰을 통해 $J_{L/w_1}$ 을 구하면 됨
$$\Delta L = J_{L/w_1} \Delta w_1$$
예시
$$J_{L/w_1} = J_{L/h_1}J_{h_1/w_1}$$
$$w_1 : = w_1 - \lambda J_{L/w_1}$$
### 6. 싸게 계산하는 법
