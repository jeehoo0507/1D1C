### 코드
---
선형 회귀 분석에서 model의 입력값 개수에만 변화가 존재하므로
추론에 관한 코드를 작성하겠다.

```python
with torch.no_grad():
    new_input = torch.FloatTensor([[75, 85, 72]])  # 예측하고 싶은 임의의 입력
    prediction = new_input.matmul(W) + b
    print('Predicted : {}'.format(prediction.item()))
```
`with torch.no_grad():` 역전파 비활성화
