
## 조건부 확률($p$)

조건부 확률
$$p(y \mid x)$$
정의 : x가 주어졌을 때 y의 확률

예시
$$x = img, \ \ y = cat$$
$$p(y=cat \mid x)$$
이미지가 주어졌을 때  고양이 일 확률

신경망에서의 조건부 확률

$$p_\theta(y \mid x)$$
여기서 $\theta$는 신경망의 파라미터 전체이다.

정의 : 파라미터 $\theta$ 를 가진 모델이 $x$를 보고 $y$의 확률을 예측

예를 들어
$$z = W_2(W_1 x+b_1)+b_2$$
라 하면
$$\theta = \{ W_1, W_2, b_1, b_2\}$$


## 정책($\pi$)

$$\pi(a|s)$$
s : 현재 상태
a : 행동
$\pi$ : 정책

정의 : 싱테 s에서 행동 a를 선택 할 확률

$$\pi_\theta(a \mid s)$$
이 또한 파리미터 $\theta$를 가진 모델의 정책을 의미한다.


기본적으로 위 개념 모두 확률 분포를 출력하는데
쉽게 생각하면 softmax 함수의 출력이라 생각하면 된다. 그 상태에서 y가 정해지면 그 값이 정해지는 것

## y = $\cdot$

$$\pi(\cdot \mid x)$$
로 적는 경우가 있는데 이는 입력 x가 주어졌을 때 가능한 출력들에 대한 확률분포
위에서 말한 소프트 맥스 함수의 출력 벡터를 의미한다.

## E

딥러닝에서 $E$ 기댓값은 가중 평균을 의미한다. 
$$
X =
\begin{cases}
2 & 50\% \\
4 & 30\% \\
10 & 20\%
\end{cases}
$$
$$
\mathbb{E}[X]
= 0.5(2) + 0.3(4) + 0.2(10)
= 4.2
$$

다만 확률 분포를 구하기 어려운 LLM 의 경우
여러번 해본 뒤 평균을 내는 방식으로 근사한다.

$$\max_{\pi_\varphi} \mathbb{E}_{(X_i, Y_i, s_i) \sim \mathcal{D}_v,\ (V_i', s_i') \sim \pi_\varphi(\cdot \mid X_i, Y_i)}
\left[ R_{\text{format}}(V_i') \cdot R_{\text{score}}(s_i', s_i) \right]$$

이 경우 한 정책에 대해 여러번 샘플링 한 뒤 평균을 낸다.